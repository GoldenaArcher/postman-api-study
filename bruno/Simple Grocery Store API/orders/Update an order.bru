meta {
  name: Update an order
  type: http
  seq: 3
}

patch {
  url: {{baseUrl}}/orders/:orderId
  body: json
  auth: bearer
}

params:path {
  orderId: ord_123456789
}

auth:bearer {
  token: {{accessToken}}
}

body:json {
  {
      "comment": "Update order id"
  }
}
