meta {
  name: Create an order
  type: http
  seq: 1
}

post {
  url: {{baseUrl}}/orders
  body: json
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

body:json {
  {
    "customerName": "{{$randomFullName}}",
    "cartId": "ZFe4yhG5qNhmuNyrbLWa4"
  }
}
